[project]
name = "mergedna"
version = "0.1.0"
description = "Merge DNA: Hierarchical Token Merging for DNA Sequences"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
    "numpy>=1.24.0",
    "tqdm",
]

[tool.uv]
package = false